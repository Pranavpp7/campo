[pytest]
asyncio_mode = auto
testpaths = evals
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast tests, no external services needed
    integration: requires Redis + Qdrant
    e2e: requires full stack